import { Meta, Story, Canvas, Controls } from '@storybook/addon-docs';
import { Input } from "..";

export const Template = ({isError, ...props}) => {
  return <Input {...{isError, ...props}}/>
}

<Meta
  title="Foundation/Input"
  component={Input}
  argTypes={{
    value: {
      control: 'text'
    },
    type: { control: "select", options: ["input", "textarea", 'email'] },
    label: {
      control: 'text'
    },
    isError: {
      control: 'boolean'
    }
  }} args={{
    label: 'Example',
    isError: false,
    disabled: false,
    value: ''
  }}
/>

# Input

Let users insert text.

<Canvas withToolbar>
  <Story name='No arguments'>
    {Template.bind({})}
  </Story>
</Canvas>

<Controls />

#### __Has a value__

<Canvas>
  <Story name="Value" args={{value: 'value'}}>
    {Template.bind({})}
  </Story>
</Canvas>

### __Is disabled__

<Canvas>
  <Story name="Disabled" args={{disabled: true}}>
    {Template.bind({})}
  </Story>
</Canvas>

#### __Is a textarea type__

<Canvas>
  <Story name="Textarea" args={{type: 'textarea'}}>
    {Template.bind({})}
  </Story>
</Canvas>



#### __Has an error__

<Canvas>
  <Story name="Error" args={{isError: true}}>
    {Template.bind({})}
  </Story>
</Canvas>
