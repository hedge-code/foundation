import { Meta, Story, Canvas, Controls } from '@storybook/addon-docs';
import { Input } from "..";

export const Template = ({label, value, isError, disabled, type}) => {
  return <Input {...{label, value, isError, disabled, type}}/>
}

<Meta
  title="Foundation/Input"
  component={Input}
  argTypes={{
    type: { control: "select", options: ["input", "textarea", 'email'] },
    label: {
      control: 'text'
    },
    isError: {
      control: 'boolean'
    }
  }} args={{
    label: 'Example',
    isError: false,
    disabled: false
  }}
/>

# Input

Let users insert text.

<Canvas withToolbar>
  <Story name='Basic'>
    {Template.bind({})}
  </Story>
</Canvas>

<Controls />

### __Is a textarea__

<Canvas>
  <Story name="Textarea" args={{type: 'textarea'}}>
    {Template.bind({})}
  </Story>
</Canvas>

### __Has an init value__

<Canvas>
  <Story name="Value" args={{value: 'value'}}>
    {Template.bind({})}
  </Story>
</Canvas>

### __Has an error__

<Canvas>
  <Story name="Error" args={{isError: true}}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Error and value" args={{isError: true, value: 'op2'}}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Error textarea" args={{isError: true, type: 'textarea'}}>
    {Template.bind({})}
  </Story>
</Canvas>