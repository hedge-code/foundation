import { Meta, Story, Canvas, Controls } from '@storybook/addon-docs';
import { Select } from "..";

export const Template = ({label, value, isLoading, isError, multiple, size, disabled}) => {
  return <Select label={label} value={value} isLoading={isLoading} isError={isError} multiple={multiple} size={size} disabled={disabled}>
      <option value='op1'>First Option</option>
      <option value="op2">Secound Option</option>
      <option value="op3">Third Option</option>
    </Select>
}

export const TemplateMoreOptions = ({label, value, isLoading, isError, multiple, size, disabled}) => {
  return <Select label={label} value={value} isLoading={isLoading} isError={isError} multiple={multiple} size={size} disabled={disabled}>
      <option value='op1'>First Option</option>
      <option value="op2">Secound Option</option>
      <option value="op3">Third Option</option>
      <option value="op4">Fourth Option</option>
      <option value="op4">Fifth Option</option>
    </Select>
}

<Meta
  title="Foundation/Select"
  component={Select}
  argTypes={{
    label: {
      control: 'text'
    },
    isLoading: {
      control: 'boolean'
    },
    isError: {
      control: 'boolean'
    },
    multiple: {
      control: 'boolean'
    }
  }} args={{
    label: 'Example',
    isLoading: false,
    isError: false,
    multiple: false,
    disabled: false,
    size: 0
  }}
/>

# Select

Let users select one option from a dropdown list.

<Canvas withToolbar>
  <Story name='Basic'>
    {Template.bind({})}
  </Story>
</Canvas>

<Controls />

### __Allows to select multiple values__

<Canvas>
  <Story name="multiple" args={{multiple: true}}>
    {Template.bind({})}
  </Story>
</Canvas>

### __Allows to select multiple with more values__

<Canvas>
  <Story name="multiple more options" args={{multiple: true}}>
    {TemplateMoreOptions.bind({})}
  </Story>
</Canvas>

### __Has an init value__

<Canvas>
  <Story name="Value" args={{value: 'op2'}}>
    {Template.bind({})}
  </Story>
</Canvas>

### __When isLoading__

<Canvas>
  <Story name="Loading" args={{isLoading: true}}>
    {Template.bind({})}
  </Story>
</Canvas>

### __Is loading and has init value__

<Canvas>
  <Story name="Loading and value" args={{isLoading: true, value: 'op2'}}>
    {Template.bind({})}
  </Story>
</Canvas>

### __Has an error__

<Canvas>
  <Story name="Error" args={{isError: true}}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Error and value" args={{isError: true, value: 'op2'}}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Loading and error" args={{isError: true, isLoading: true}}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="All" args={{isError: true, isLoading: true, value: 'op2'}}>
    {Template.bind({})}
  </Story>
</Canvas>