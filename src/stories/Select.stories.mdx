import { Meta, Story, Canvas, Controls } from '@storybook/addon-docs';
import { Select } from "../components";

export const Template = ({label, value, isLoading, isError}) => {
  return <Select label={label} value={value} isLoading={isLoading} isError={isError}>
      <option value='op1'>Option First</option>
      <option value="op2">Option Secound</option>
    </Select>
}

<Meta
  title="Foundation/Select"
  component={Select}
  argTypes={{
    label: {
      control: 'text'
    },
    isLoading: {
      control: 'boolean'
    },
    isError: {
      control: 'boolean'
    }
  }} args={{
    label: 'Example',
    isLoading: false,
    isError: false
  }}
/>

# Select

Let users select one option from a dropdown list.

<Canvas>
  <Story name='Basic'>
    {Template.bind({})}
  </Story>
</Canvas>

<Controls />

### __Has an init value__

<Canvas>
  <Story name="Value" args={{value: 'op2'}}>
    {Template.bind({})}
  </Story>
</Canvas>

### __When isLoading__

<Canvas>
  <Story name="Loading" args={{isLoading: true}}>
    {Template.bind({})}
  </Story>
</Canvas>

### __Is loading and has init value__

<Canvas>
  <Story name="Loading and value" args={{isLoading: true, value: 'op2'}}>
    {Template.bind({})}
  </Story>
</Canvas>

### __Has an error__

<Canvas>
  <Story name="Error" args={{isError: true}}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Error and value" args={{isError: true, value: 'op2'}}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Loading and error" args={{isError: true, isLoading: true}}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="All" args={{isError: true, isLoading: true, value: 'op2'}}>
    {Template.bind({})}
  </Story>
</Canvas>
