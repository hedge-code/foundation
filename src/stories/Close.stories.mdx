import { Meta, Story, Canvas, Controls } from '@storybook/addon-docs';
import { CloseIcon } from "../components";
import classNames from 'classnames'

export const Template = ({className}) => {
  return <div className={classNames('h-16', className)}><CloseIcon /></div>
}

<Meta title="Foundation/Close" component={CloseIcon}/>

# Close Icon

Fully responsive.

<Canvas>
  <Story name='Basic'>
    {Template.bind({})}
  </Story>
</Canvas>

Color inherited from text.

<Canvas>
  <Story name='Red' args={{className: "text-red-500"}}>
    {Template.bind({})}
  </Story>
</Canvas>