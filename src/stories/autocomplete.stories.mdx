import { Meta, Story, Canvas, Controls } from '@storybook/addon-docs';
import { Autocomplete } from "../components";

export const Template = () => {
  return <Autocomplete suggestions={['mainstream', 'suntan', 'congress', 'boom', 'find', 'balance', 'discovery', 'warning', 'eavesdrop', 'clay', 'countryside', 'thumb', 'pyramid', 'reporter', 'cage', 'few', 'temptation']} />
}

<Meta
  title="Foundation/Autocomplete"
  component={Autocomplete}
  argTypes={{}} args={{}}
/>

# Autocomplete

Let users insert text.

<Canvas withToolbar>
  <Story name='Basic'>
    {Template.bind({})}
  </Story>
</Canvas>

<Controls />
